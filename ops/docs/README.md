# Documentation

Documentation quality has two contracts here: Markdown must remain readable,
and every runnable example must have a local explanation plus an entry in the
example index. This makes executable learning material discoverable instead of
merely present on disk.

Run `just ops docs all` after adding or restructuring examples.
