# Runtime evidence

This capability keeps two complementary forms of behavior evidence separate:
Pest tests exercise programmatic contracts, while the examples demonstrate the
runtime's intended developer experience end to end.

Use `just ops test unit` or `just ops test examples` when focusing on one
signal. Both are included in the derived repository test lane.
