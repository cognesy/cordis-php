---
name: Tests

on:  # yamllint disable-line rule:truthy
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  repository-gate:
    name: Repository gate (PHP 8.4)
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          coverage: none
          tools: composer:v2

      - name: Set up Node.js for documentation tooling
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Markdown linting tool
        run: npm install --global markdownlint-cli

      - name: Install Just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install YAML Schema validator
        run: cargo install yaml-schema --locked

      - name: Install locked Composer dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Run the complete repository gate
        run: just ci

  compatibility:
    name: >-
      PHP ${{ matrix.php }} / Symfony YAML ${{ matrix.symfony }} /
      ${{ matrix.dependencies }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ["8.2", "8.3", "8.4", "8.5"]
        symfony: ["^7.3", "^8.0"]
        dependencies: ["--prefer-stable", "--prefer-lowest"]
        exclude:
          - php: "8.2"
            symfony: "^8.0"
          - php: "8.3"
            symfony: "^8.0"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - name: Install Just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Validate the committed dependency contract
        run: composer validate --strict

      - name: Resolve the matrix dependency line
        run: |
          composer config --unset platform.php
          composer update --no-interaction --prefer-dist --no-progress \
            ${{ matrix.dependencies }} \
            --with "symfony/yaml:${{ matrix.symfony }}" \
            --with-all-dependencies

      - name: Run the compatibility test capability
        run: just test
